# C++版技术解析文档 - 组长（架构设计与管理）

## 一、职责概述

组长负责C++版本的整体架构设计、技术选型决策、模块划分、代码规范制定和项目协调管理。在C++版本中，组长需要将原Vue+Flask架构迁移到Qt6框架，设计分层架构，确保功能完整性和代码质量。

---

## 二、架构设计

### 2.1 系统架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    Vue 3 前端应用                         │
│         (与Flask版本共享同一前端代码)                       │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP/JSON (RESTful API)
                         ▼
┌─────────────────────────────────────────────────────────┐
│                  Qt6 HTTP Server                         │
│              (QHttpServer + QTcpServer)                  │
├─────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │CourseApi│ │ RoleApi │ │ AuthApi │ │ AiApi   │ ...   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘       │
│       │           │           │           │             │
├───────┴───────────┴───────────┴───────────┴─────────────┤
│                    Service 层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │CourseSvc │ │ RoleSvc  │ │ AuthSvc  │ │ AiSvc    │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘   │
│       │            │            │             │          │
├───────┴────────────┴────────────┴─────────────┴─────────┤
│                     DAO 层                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │CourseDao │ │ RoleDao  │ │ UserDao  │ │GoalDao   │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘   │
│       │            │            │             │          │
├───────┴────────────┴────────────┴─────────────┴─────────┤
│              Qt6 SQL (QSqlDatabase)                      │
│                   SQLite                                 │
└─────────────────────────────────────────────────────────┘
```

### 2.2 分层架构设计

| 层次 | 目录 | 职责 | 关键类 |
|------|------|------|--------|
| API层 | src/api/ | HTTP请求处理、路由映射 | CourseApi, AuthApi, DashboardApi等 |
| Service层 | src/service/ | 业务逻辑、数据计算 | CourseService, AuthService, AiService等 |
| DAO层 | src/dao/ | 数据库CRUD操作 | CourseDao, UserDao, GoalDao等 |
| Model层 | src/model/ | 数据模型定义、序列化 | Course, Role, Achievement等 |
| Server层 | src/server/ | HTTP服务器、路由注册 | HttpServer |
| Util层 | src/util/ | 工具类 | JsonUtils, Logger |

### 2.3 数据流

```
HTTP Request
    → QHttpServer (路由匹配)
    → XxxApi (请求解析、响应封装)
    → XxxService (业务逻辑)
    → XxxDao (数据库操作)
    → QSqlQuery (SQL执行)
    → SQLite (数据存储)
    → 返回结果沿原路返回
    → HTTP Response (JSON)
```

---

## 三、技术选型决策

### 3.1 核心技术选型

| 决策项 | 选择 | 备选方案 | 选择理由 |
|--------|------|----------|----------|
| 框架 | Qt6 | Crow, oat++, Pistache | Qt生态完整，内置HTTP Server和SQL模块 |
| HTTP服务器 | QHttpServer | QTcpServer手动实现 | 内置路由、请求解析，开发效率高 |
| 数据库 | SQLite | MySQL, PostgreSQL | 嵌入式、零配置、与Flask版本一致 |
| JSON处理 | QJsonObject | nlohmann/json | Qt原生支持，与QHttpServer无缝集成 |
| 构建系统 | CMake | QMake, xmake | 跨平台标准，Qt6官方推荐 |
| 语言标准 | C++17 | C++20, C++14 | 平衡兼容性和现代特性 |

### 3.2 设计模式选择

| 模式 | 应用场景 | 实现方式 |
|------|----------|----------|
| Header-Only | Model/Service/DAO/Api | 直接在.h中实现，减少.cpp文件 |
| 静态方法 | Service/Api类 | 无状态，无需实例化 |
| toDict/fromDict | 数据序列化 | 统一的JSON序列化接口 |
| DAO模式 | 数据访问 | 隔离SQL操作 |
| 中间件 | CORS处理 | afterRequest钩子 |

---

## 四、模块划分与依赖关系

### 4.1 模块依赖图

```
main.cpp
  ├── HttpServer ──────→ 所有Api类
  │                       ├── CourseApi ──→ CourseService ──→ CourseDao
  │                       ├── RoleApi ────→ RoleService ────→ RoleDao
  │                       ├── AuthApi ────→ AuthService ────→ UserDao
  │                       ├── DashboardApi → DashboardService → 多个Dao
  │                       ├── AnalyticsApi → AnalyticsService → 多个Dao
  │                       ├── AiApi ──────→ AiService ──────→ 多个Service
  │                       ├── ResumeApi ──→ ResumeService ──→ 多个Dao
  │                       ├── ImportApi ──→ ImportService ──→ 多个Service
  │                       └── ...
  ├── DaoBase ──────────→ QSqlDatabase
  └── initializeDatabase → schema.sql
```

### 4.2 文件统计

| 类别 | 文件数 | 代码行数(约) |
|------|--------|-------------|
| Model | 10个.h + 1个.cpp | ~800行 |
| DAO | 1个.h/.cpp基类 + 9个.h | ~600行 |
| Service | 13个.h | ~1200行 |
| API | 14个.h | ~700行 |
| Server | 1个.h | ~200行 |
| Util | 2个.h | ~100行 |
| Main | 1个.cpp | ~50行 |
| Schema | 1个.sql | ~150行 |
| CMake | 1个CMakeLists.txt | ~36行 |
| **合计** | **~50个文件** | **~3800行** |

---

## 五、核心架构组件详解

### 5.1 HttpServer (server/HttpServer.h)

**架构角色：** 系统入口、路由注册中心、CORS中间件

**创建的核心对象：**

| 对象 | 类型 | 职责 |
|------|------|------|
| `m_server` | QHttpServer | HTTP请求处理引擎 |
| `m_tcpServer` | QTcpServer | TCP端口监听 |

**路由注册策略：**

1. **精确路径优先**：`/api/courses/statistics` 先于 `/api/courses` 注册
2. **参数路由使用 `<arg>`**：`/api/courses/<arg>` 匹配动态ID
3. **HTTP方法分离**：同一路径不同方法分别注册
4. **CORS全局处理**：afterRequest中间件统一添加CORS头

**完整路由注册代码结构：**

```cpp
void registerRoutes() {
    // CORS中间件
    m_server.afterRequest([](QHttpServerResponse&& resp) {
        resp.setHeader("Access-Control-Allow-Origin", "*");
        resp.setHeader("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS");
        resp.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization, X-Auth-Token");
        return std::move(resp);
    });

    // 课程路由
    m_server.route("/api/courses/statistics", Get, CourseApi::getStatistics);
    m_server.route("/api/courses", Get, CourseApi::getAll);
    m_server.route("/api/courses", Post, CourseApi::create);
    m_server.route("/api/courses/<arg>", Get, CourseApi::getById);
    m_server.route("/api/courses/<arg>", Put, CourseApi::update);
    m_server.route("/api/courses/<arg>", Delete, CourseApi::remove);
    
    // ... 14个模块的路由注册
}
```

### 5.2 DaoBase (dao/DaoBase.h / DaoBase.cpp)

**架构角色：** 数据库连接管理、SQL执行基类

**单例连接模式：**

```cpp
QSqlDatabase DaoBase::getDatabase() {
    if (!QSqlDatabase::contains("pdp_connection")) {
        QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE", "pdp_connection");
        db.setDatabaseName("pdp.db");
        db.open();
    }
    return QSqlDatabase::database("pdp_connection");
}
```

### 5.3 JsonUtils (util/JsonUtils.h)

**架构角色：** 统一响应格式封装

**标准响应格式：**

```json
{
    "success": true/false,
    "code": 200/400/404/201,
    "message": "操作描述",
    "data": { ... } / [ ... ]
}
```

---

## 六、数据库架构设计

### 6.1 Schema设计 (resources/schema.sql)

**10个核心表：**

| 表名 | 主键 | 外键 | 索引 |
|------|------|------|------|
| courses | id | - | code, semester, status |
| roles | id | - | type, is_active |
| achievements | id | - | type, level, date |
| experiences | id | - | type, is_ongoing |
| activities | id | - | start_date |
| goals | id | - | category, status, priority |
| jobs | id | - | status |
| job_requirements | id | job_id → jobs.id | job_id |
| users | id | - | username(UNIQUE), email(UNIQUE) |
| peer_benchmarks | id | - | semester |

### 6.2 数据库初始化流程

```cpp
// main.cpp
bool initializeDatabase() {
    QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE");
    db.setDatabaseName("pdp.db");
    if (!db.open()) return false;
    
    // 读取并执行schema.sql
    QFile schemaFile(":/resources/schema.sql");
    schemaFile.open(QIODevice::ReadOnly);
    QString schema = schemaFile.readAll();
    QStringList statements = schema.split(";", Qt::SkipEmptyParts);
    for (const auto& stmt : statements) {
        QSqlQuery query(db);
        query.exec(stmt.trimmed());
    }
    return true;
}
```

---

## 七、C++与Vue+Flask版本架构对比

### 7.1 整体架构对比

| 对比项 | Vue+Flask版本 | C++版本 |
|--------|--------------|---------|
| 前端 | Vue 3 + Vite | 共享同一前端 |
| 后端框架 | Flask 2.3 | Qt6 HttpServer |
| ORM | SQLAlchemy | 手动SQL (QSqlQuery) |
| 序列化 | marshmallow/to_dict | QJsonObject toDict/fromDict |
| 路由 | Flask Blueprint | QHttpServer route |
| 认证 | Flask-JWT-Extended | 自定义Token (SHA256) |
| CORS | Flask-CORS | afterRequest中间件 |
| 数据库迁移 | Flask-Migrate | schema.sql初始化 |
| 配置管理 | python-dotenv | 硬编码/编译时配置 |
| 运行方式 | python app.py | ./pdp_server |

### 7.2 功能完整性对比

| 功能模块 | Flask版本 | C++版本 | 状态 |
|----------|----------|---------|------|
| 课程CRUD | ✅ | ✅ | 完全一致 |
| 角色CRUD | ✅ | ✅ | 完全一致 |
| 成就CRUD | ✅ | ✅ | 完全一致 |
| 经验CRUD | ✅ | ✅ | 完全一致 |
| 活动CRUD | ✅ | ✅ | 完全一致 |
| 目标CRUD | ✅ | ✅ | 完全一致 |
| 岗位CRUD | ✅ | ✅ | 完全一致 |
| 用户认证 | ✅ | ✅ | 完全一致 |
| 仪表盘 | ✅ | ✅ | 完全一致 |
| 分析中心 | ✅ | ✅ | 完全一致 |
| 时间轴 | ✅ | ✅ | 完全一致 |
| 简历生成 | ✅ | ✅ | 完全一致 |
| 数据导入 | ✅ | ✅ | 完全一致 |
| AI助手 | ✅ 规则+LLM | ✅ 规则引擎 | C++版仅规则模式 |
| GPA计算 | ✅ | ✅ | 完全一致 |
| 同学对比 | ✅ | ✅ | 完全一致 |

### 7.3 API兼容性

C++版本的HTTP服务器与Flask版本共享**完全相同的API接口**，前端无需任何修改即可切换后端：

- 相同的URL路径（`/api/courses`, `/api/auth/login`等）
- 相同的请求/响应JSON格式
- 相同的HTTP状态码
- 相同的CORS配置

---

## 八、项目构建与部署

### 8.1 CMakeLists.txt 设计

```cmake
cmake_minimum_required(VERSION 3.25)
project(pdp_server LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)

find_package(Qt6 REQUIRED COMPONENTS Core Sql HttpServer Network)

add_executable(pdp_server
    src/main.cpp
    src/model/Course.cpp
    src/dao/DaoBase.cpp
)

target_link_libraries(pdp_server PRIVATE
    Qt6::Core Qt6::Sql Qt6::HttpServer Qt6::Network
)

target_include_directories(pdp_server PRIVATE ${PROJECT_SOURCE_DIR}/src)

file(COPY ${CMAKE_SOURCE_DIR}/resources/schema.sql
     DESTINATION ${CMAKE_BINARY_DIR}/resources)
```

### 8.2 构建流程

```bash
# 1. 配置（需要Qt6环境）
cmake -B build -S . -G "MinGW Makefiles" ^
      -DCMAKE_PREFIX_PATH=C:/Qt/6.x.x/mingw_64

# 2. 编译
cmake --build build

# 3. 运行
./build/pdp_server
```

### 8.3 部署要求

| 依赖 | 最低版本 | 用途 |
|------|----------|------|
| Qt6 Core | 6.2+ | 核心库 |
| Qt6 Sql | 6.2+ | 数据库驱动 |
| Qt6 HttpServer | 6.2+ | HTTP服务器 |
| Qt6 Network | 6.2+ | 网络通信 |
| SQLite | 3.x | 数据库（Qt内置驱动） |
| C++17编译器 | - | MSVC 2019+/GCC 9+/Clang 10+ |

---

## 九、代码规范

### 9.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类名 | PascalCase | CourseService, CourseApi |
| 方法名 | camelCase | getAll, getById, create |
| 成员变量 | camelCase + m_前缀 | m_server, m_tcpServer |
| 局部变量 | camelCase | course, result, query |
| 常量 | UPPER_SNAKE_CASE | MAX_RETRY_COUNT |
| 头文件保护 | _H后缀 | #ifndef COURSESERVICE_H |
| 文件名 | PascalCase | CourseService.h |

### 9.2 文件组织规范

- 每个类一个头文件
- Header-Only优先（简单实现直接写在.h中）
- 复杂实现拆分为.h/.cpp对
- #include顺序：Qt头文件 → 项目头文件

### 9.3 错误处理规范

```cpp
// DAO层：返回空对象表示未找到
Course CourseDao::getById(int id) {
    Course c;
    // ... 查询
    if (query.next()) { /* 填充c */ }
    return c;  // c.id == 0 表示未找到
}

// Service层：检查id判断成功
Course CourseService::update(int id, Course& course) {
    Course existing = dao.getById(id);
    if (existing.id == 0) return existing;  // 未找到
    // ... 更新
}

// API层：返回适当的HTTP响应
QHttpServerResponse CourseApi::getById(int id) {
    Course c = CourseService::getById(id);
    if (c.id == 0) return JsonUtils::notFoundResponse("课程不存在");
    return JsonUtils::successResponse(c.toDict());
}
```

---

## 十、团队分工与协调

### 10.1 C++版本团队分工

| 角色 | 成员 | 主要负责 |
|------|------|----------|
| 组长 | 架构设计与管理 | 整体架构、技术选型、模块划分、代码审查 |
| 组员A | API层与路由开发 | 14个API类、HttpServer路由注册、JsonUtils、CORS |
| 组员B | 后端核心开发 | 10个Model类、9个DAO类、13个Service类、Schema |
| 组员C | 测试与文档 | 单元测试、API测试、数据库测试、日志系统、文档 |

### 10.2 代码贡献统计

| 成员 | 文件数 | 代码行数(约) | 占比 |
|------|--------|-------------|------|
| 组员A | 16个 | ~1000行 | 26% |
| 组员B | 33个 | ~2600行 | 68% |
| 组员C | 测试+文档 | ~800行 | 6% |
| 组长 | 架构+协调 | 贯穿全部 | - |

---

## 十一、项目质量指标

### 11.1 代码质量

| 指标 | 目标 | 当前状态 |
|------|------|----------|
| 编译零错误 | 0 errors | ✅ |
| 编译零警告 | 0 warnings | ✅ |
| API兼容性 | 100% | ✅ |
| 功能覆盖率 | 100% | ✅（AI仅规则模式） |
| 代码规范一致性 | 100% | ✅ |

### 11.2 架构质量

| 指标 | 评价 |
|------|------|
| 分层清晰度 | ★★★★★ API-Service-DAO-Model四层清晰 |
| 模块耦合度 | ★★★★☆ Service间有少量依赖（DashboardService） |
| 代码复用性 | ★★★★☆ DAO基类、JsonUtils工具类复用良好 |
| 可扩展性 | ★★★★☆ 新增模块只需添加Model+DAO+Service+Api |
| 可测试性 | ★★★★☆ 静态方法便于测试，但DAO层需数据库 |

---

## 十二、总结

C++版本成功将Vue+Flask后端迁移到Qt6框架，实现了：

1. **架构一致性**：保持API-Service-DAO-Model四层架构
2. **API兼容性**：50+个API端点与Flask版本完全一致
3. **功能完整性**：14个功能模块全部实现
4. **代码质量**：Header-Only设计、统一命名规范、标准错误处理
5. **构建简洁**：CMake单文件配置，3个编译源文件

组长在架构设计中的核心贡献：
- 选择Qt6作为C++框架，平衡了开发效率和运行性能
- 设计Header-Only模式，简化项目结构
- 制定toDict/fromDict序列化标准，统一数据转换接口
- 设计统一响应格式（JsonUtils），确保API一致性
- 规划CORS中间件方案，确保前后端分离架构正常工作